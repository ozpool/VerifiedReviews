// @vr/web — Next.js (App Router) + Wagmi frontend.
// The app is scaffolded in PR #10 (feat/issue-10-web-foundation).

import { PROJECT_NAME } from '@vr/shared';

export const WEB_NAME = `${PROJECT_NAME} Web`;
