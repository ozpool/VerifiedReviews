// @vr/api — Express + TypeScript backend.
// The server bootstraps in PR #6 (feat/issue-6-api-foundation).

import { PROJECT_NAME } from '@vr/shared';

export const API_NAME = `${PROJECT_NAME} API`;
