# @vr/contracts

Solidity contracts for VerifiedReviews, built and tested with Hardhat.

- `VisitProofSBT.sol` — non-transferable visit receipt (ERC-5192). Added in PR #3.
- `ReviewRegistry.sol` — SBT-holder + 60-day recency gate. Added in PR #4.

Hardhat config, deps, and Arbitrum Sepolia deployment land in PRs #3–#5.
